# 🦖 InstallProtect v8 (Anti Akses Server Controller)

Script ini memasang proteksi agar server tidak bisa dihapus sembarangan di panel Pterodactyl.

## 🚀 Install
Jalankan sebagai root:

```bash
curl -fsSL https://raw.githubusercontent.com/aretaazhar05/install-v8.sh-by-official/main/install-v8.sh | sudo bash
